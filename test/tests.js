import chai from 'chai';
const expect = chai.expect;

import { bookingsSampleData } from '../src/data-sample/bookings-sample';
import { roomsSampleData } from '../src/data-sample/rooms-sample';
import { customersSampleData  } from '../src/data-sample/customer-sample';

describe('See if the tests are running', function() {
  it('should return true', function() {
    expect(true).to.equal(true);
  });
});
